// Minimal JS: could expand for interactions
document.addEventListener('DOMContentLoaded', ()=>{
  // placeholder: future interactivity (menu toggles, filtering users...)
  console.log('Demo UI ready')
});
